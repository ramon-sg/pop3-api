{
  "name": "pop3-api",
  "module": "index.ts",
  "type": "module",
  "version": "0.0.1",
  "scripts": {
    "clear": "rm -rf dist",
    "build": "bun run clear && bun build src/index.ts --outdir dist --target bun",
    "start": "bun run dist/index.js",
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "bun --watch src/index.ts"
  },
  "devDependencies": {
    "@types/bun": "latest"
  },
  "peerDependencies": {
    "typescript": "^5.0.0"
  },
  "dependencies": {
    "log4js": "^6.9.1",
    "node-pop3": "^0.9.0",
    "postal-mime": "^2.2.8"
  }
}
